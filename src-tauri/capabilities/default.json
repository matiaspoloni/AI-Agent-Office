{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Permissions for the main window. Only core APIs and the folder picker are exposed to the UI.",
  "windows": ["main"],
  "permissions": ["core:default", "dialog:allow-open"]
}
